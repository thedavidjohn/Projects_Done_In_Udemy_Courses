import { TestDirectoryDirective } from './test-directory.directive';

describe('TestDirectoryDirective', () => {
  it('should create an instance', () => {
    const directive = new TestDirectoryDirective();
    expect(directive).toBeTruthy();
  });
});
